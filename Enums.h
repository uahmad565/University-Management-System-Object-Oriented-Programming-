#pragma once
enum Grade
{
	A = 1, B, C, D, F, W
};

enum Role
{
	HEAD_OF_DEPARTMENT = 1, TEACHER, IT_MANAGER, STUDENT
};

enum Colors
{
	YELLOW = 6,
	WHITE = 7,
	GREEN = 10,
	RED = 12,
};